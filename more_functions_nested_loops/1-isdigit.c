#include "main.h"

/**
 *  * _isdigit - returns 1 if c is a digit
 *   * @c: digit
 *    * Return: 0
 *     */

int _isdigit(int c)
{
		if (c < 48 || c > 57)
				{
							return (0);
								}
			else
					{
								return (1);
									}
}

#include <stdio.h>

/**
 *  main - Displays the whole alphabet in lowercase
 *
 * Return: 0 in Succes
 *
 * */

int main(void)
{
		char ch = 'a';
			char cj = 'A';

	while (ch <= 'z')
	{
	putchar(ch)
	(ch++);
}
	while (cj <= 'Z')
{
	putchar(cj);
	(cj++);
}
	putchar('\n');
	return (0);
}
